# Technology Stack

**Analysis Date:** 2025-03-04

## Languages

**Primary:**
- Dart ^3.9.0 - Main application logic and UI code (`lib/`)

**Secondary:**
- Kotlin - Android platform-specific implementation (`android/`)

## Runtime

**Environment:**
- Flutter - UI Framework
- JDK 21 - Android build environment (`.idx/dev.nix`)

**Package Manager:**
- Pub - Dart/Flutter package manager
- Lockfile: `pubspec.lock` present
- npm - Node package manager (for development tools like Supabase)
- Lockfile: `package-lock.json` present

## Frameworks

**Core:**
- Flutter - Mobile and web application framework
- Provider ^6.1.5+1 - State management and dependency injection (`lib/main.dart`)

**Testing:**
- flutter_test (sdk) - Unit and widget testing (`test/widget_test.dart`)

**Build/Dev:**
- flutter_lints ^5.0.0 - Linting and code quality

## Key Dependencies

**Critical:**
- firebase_core ^4.9.0 - Firebase initialization
- firebase_auth ^6.5.1 - User authentication
- cloud_firestore ^6.4.1 - Real-time database
- google_fonts ^8.1.0 - Custom typography

**Infrastructure:**
- http ^1.6.0 - General HTTP requests
- intl ^0.20.2 - Internationalization and formatting

## Configuration

**Environment:**
- Firebase configuration via `lib/firebase_options.dart` (generated by FlutterFire)
- Firebase project configuration in `firebase.json`

**Build:**
- `pubspec.yaml` - Flutter project configuration
- `android/build.gradle.kts` - Android build configuration
- `.idx/dev.nix` - Project environment and tools

## Platform Requirements

**Development:**
- Flutter SDK
- Android SDK (JDK 21)
- Firebase CLI (for backend configuration)

**Production:**
- Android
- Web (configured in `.idx/dev.nix` but `firebase_options.dart` currently lacks web config)

---

*Stack analysis: 2025-03-04*
